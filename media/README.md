# media

**`executor-demo-v5-silent.mp4` — 3:51 — is the submission video.**

Record your voice over it using `docs/RECORD_IT_YOURSELF.md`. It has no audio
track, which is deliberate: ETHGlobal prohibits TTS and AI voiceover.

Everything in it is real and was made during the take — `registerAgent` from a
clicked button, three heartbeats, and `enterAdministration` once the window
lapsed, which moved the payment destination from the treasury to the estate on
camera.

## archive/

Every earlier cut, and every AI-voice timing guide. **None of them should be
submitted or used as a reference**, and they are here rather than deleted only
so the history is recoverable.

They are easy to open by mistake and hard to tell apart, which has already
happened once: the original `executor-demo-silent.mp4` is 3:33 and still says
`200,000 USDC`, because it predates the fix that made the figure match what
Etherscan shows. If a video seems wrong, check its duration first — the
submission video is **3:51** and nothing else is.
