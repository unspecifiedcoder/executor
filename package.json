{
  "name": "executor",
  "private": true,
  "version": "0.1.0",
  "description": "A living will for autonomous agents: dead-man's-switch succession, on-chain waterfall payouts, and confidential solvency checks.",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0",
    "viem": "^2.21.0"
  }
}
